# Published objects on https://assets.ha-paneld.com — one per line, tab separated.
# Generated by tools/docs-assets/assets.py; do not hand-edit.
#
# Objects are PERMANENT: an entry here may gain corrections to its metadata columns,
# but a key must never be deleted, renamed or repointed at different bytes. Historical
# commits reference these URLs and cannot be corrected.
#
# 'source' is a courtesy record of where a third-party image came from. It is expected
# to rot; the served copy is the durable artefact and nothing depends on the source.
#
# key	sha256	bytes	dimensions	provenance	source	captured	licence
