# ha-paneld info-page screenshot fixture
#
# Synthetic panel data for regenerating the README / GitHub screenshots reproducibly, WITHOUT leaking any
# real device identifier or network address. Modelled on a real NSPanel 120P, with every identity/network
# field replaced by a safe, non-routable value:
#   - Device ID         synthetic 16-hex (not a real ANDROID_ID)
#   - Local IP          RFC1918 192.168.x  (LAN-scope → NOT blurred on the page, by design)
#   - Local IPv6        RFC3849 doc range 2001:db8::/32  (global-scope → blurred on the page, by design)
#   - MQTT broker       a generic .local hostname
#   - panel_id / name   generic ("Office Panel")
# Device specs (model, fw, CPU, RAM, storage, display, sensors, Zigbee) are real and not sensitive.
#
# Format: one `Key = Value` per line, in render order. Blank lines and `#` comments are ignored.
# MANUALLY TUNABLE — edit any value, and ADD NEW `Key = Value` lines as the info page gains fields, so the
# screenshots track the UI. The renderer feeds these into the same info-page template the panel serves.
# (Sensitive fields are blurred-by-default on the page; the synthetic values here stay safe even revealed.)

Android = 8.1.0 (API 27)
Firmware = 3.7.1
Model = NSPanel 120P · fw 3.7.1
Device = rockchip px30_evb (px30_evb)
Device ID = a1b2c3d4e5f60718
CPU = 4 cores · arm64-v8a · rk30board
RAM = 1.9 GiB total · 1.3 GiB free
Storage = 7.8 GB eMMC · 2.6 GB free (data)
Display = 750×1334 · 250 dpi · 6.1″
System WebView = com.android.webview 138.0.7204.63
HA Companion = 2026.5.4-minimal (io.homeassistant.companion.android.minimal)
panel_id = panel_office
Friendly name = Office Panel
Accessibility nav = no
Local IP = 192.168.1.50
Local IPv6 = 2001:db8:ea01:3a15:b57c:b6ff:ed6c:edfc
HTTP port = 8888
MQTT = homeassistant.local · connected
mDNS = panel_office _ha-paneld._tcp.local.
Network ADB = persistent (5555) · survives reboot
Platform = Sonoff NSPanel Pro · PX30 / rk3326
LED = none
Light sensor = yes · Ambient light · Float · 0–10240 lx
Proximity = yes · Infrared · Binary · near/far (0 / 9 cm)
Zigbee = vendor-native · running · Coordinator
Relays = none
CPU profile = Auto
Root (su) = ● available
Brightness = ● WRITE_SETTINGS granted
Screen on/off = ● true backlight-off via su bl_power
RGB LED = ● no app-accessible LED node; sysfs LEDs need the root helper daemon (install needs su once)
Reboot / reload / launcher = ● available
