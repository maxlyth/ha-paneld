name: F-Droid tooling
run-name: F-Droid tooling · ${{ github.head_ref || github.ref_name }}

on:
  push:
    branches: [main]
    paths:
      - tools/fdroid/requirements.in
      - tools/fdroid/requirements.txt
      - tools/fdroid/build-requirements.in
      - tools/fdroid/build-requirements.txt
      - .github/workflows/fdroid.yml
      - .github/workflows/fdroid-tooling.yml
  pull_request:
    branches: [main]
    paths:
      - tools/fdroid/requirements.in
      - tools/fdroid/requirements.txt
      - tools/fdroid/build-requirements.in
      - tools/fdroid/build-requirements.txt
      - .github/workflows/fdroid.yml
      - .github/workflows/fdroid-tooling.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  install:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install hash-locked F-Droid tooling
        run: |
          python -m pip install --require-hashes -r tools/fdroid/build-requirements.txt
          python -m pip install --require-hashes --no-build-isolation -r tools/fdroid/requirements.txt

      - name: Verify installed F-Droid version
        run: |
          expected="$(sed -nE 's/^fdroidserver==([^[:space:]\\]+).*/\1/p' tools/fdroid/requirements.in)"
          test -n "$expected"
          test "$(fdroid --version)" = "$expected"
