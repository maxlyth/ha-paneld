*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build/
**/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
*.jks
# …except the shared debug keystore (not a secret — keeps CI build signatures stable; lives in gradle/).
!gradle/debug.keystore
keystore.properties
/.devcontainer/post-create.local.sh
/app/release/
# Compiled helper binaries (built on demand via helper/build.sh).
/helper/dist/

# Python bytecode (gen-icon.py imports)
__pycache__/
*.pyc

# cdprelay binaries are built by gradle (compileCdpRelay) from helper/cdprelay.c
/app/src/main/assets/cdprelay-arm64
/app/src/main/assets/cdprelay-arm
/app/src/main/assets/hapaneld-helper-arm64
/app/src/main/assets/hapaneld-helper-arm

# gen-icon.py preview output (dev artifact, not shipped)
docs/icon-preview.png

# Documentation image store — originals and published bytes are kept out of this repository.
/.docs-assets/

# UI layout-test deps (playwright-core)
test/node_modules/

# Documentation localization tool dependencies
/tools/docs-i18n/node_modules/

# Vendor firmware images for private hardware research — NEVER commit (large binaries; public repo).
# No trailing slash so it also ignores the symlink form (the blobs live in /media to stay out of HA
# backups; this path is symlinked there).
vendor-firmwares

# F-Droid repo build outputs — generated in CI from GitHub Releases, never committed.
tools/fdroid/repo/
tools/fdroid/archive/
tools/fdroid/tmp/
tools/fdroid/.apkcache
_site/
