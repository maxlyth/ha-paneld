*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build/
**/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
*.jks
# …except the shared debug keystore (not a secret — keeps CI build signatures stable).
!debug.keystore
keystore.properties
/app/release/
# Compiled helper binaries (built on demand via helper/build.sh).
/helper/dist/

# Python bytecode (gen-icon.py imports)
__pycache__/
*.pyc
