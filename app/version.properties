# Release identity, read by app/build.gradle.kts. Kept out of the build scripts so a version bump
# does not change the Gradle cache key or the build-file paths that start the emulator tests.
versionCode=793
versionName=0.9.8-rc1
