// Automatic entity-learning diagnostics. Same-origin/LAN guards are enforced server-side.
(function(){
  'use strict';
  var status=document.getElementById('entity-status'),search=document.getElementById('entity-search'),timer,pageSize=100,suggestedCount=0;
  var searchStatus=document.getElementById('entity-search-status'),queryGeneration=0,activeGeneration=0,generationResults=null,generationSettled=0,revealGeneration=0,settledQuery='',announcedText=null;
  var syncButton=document.getElementById('entity-sync'),applyButton=document.getElementById('entity-activate'),resetButton=document.getElementById('entity-reset'),actionResult=document.getElementById('entity-action-result'),mutationOwner=null,mutationSequence=0,lastStatus=null;
  var issuesCard=document.getElementById('entity-issues'),issuesSummary=document.getElementById('entity-issues-summary'),issuesList=document.getElementById('entity-issues-list'),issuesRescan=document.getElementById('entity-issues-rescan');
  var dynamicSection=document.getElementById('entity-dynamic'),dynamicList=document.getElementById('entity-dynamic-list');
  var autoStatic=document.getElementById('entity-auto-static'),autoRuntime=document.getElementById('entity-auto-runtime');
  function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
  function age(ts){if(!ts)return 'never';var s=Math.max(0,Math.floor((Date.now()-ts)/1000));if(s<60)return s+'s ago';if(s<3600)return Math.floor(s/60)+'m ago';if(s<86400)return Math.floor(s/3600)+'h ago';return Math.floor(s/86400)+'d ago'}
  function displayBytes(n){n=Math.max(0,Number(n)||0);if(n>=1073741824)return (n/1073741824).toFixed(1)+' GB';if(n>=1048576)return (n/1048576).toFixed(1)+' MB';if(n>=1024)return (n/1024).toFixed(1)+' KB';return Math.round(n)+' B'}
  function rate(n){n=Number(n)||0;if(n<0.01)return '0';if(n<1)return n.toFixed(2);if(n<10)return n.toFixed(1);return n.toFixed(0)}
  function ranked(value,rankValue,format){var r=Number(rankValue)||0,level=r<=1/3?'low':r<=2/3?'mid':'high';return '<span class="metric-rank rank-'+level+'">'+format(value)+'</span>'}
  function reasonIcons(e,reason){var out=[];function add(glyph,label){out.push('<span class="reason-icon" title="'+esc(label)+'" aria-label="'+esc(label)+'">'+glyph+'</span>')}if(e.static||reason.indexOf('dashboard')>=0)add('▦','Dashboard configuration');if(e.runtime||reason.indexOf('runtime')>=0)add('↯','Runtime hass.states access');if(e.pinned||reason.indexOf('manual')>=0)add('●','Manual override');if(e.excluded)add('⊘','Excluded but still detected');if(!out.length&&search.value.trim())add('⌕','Catalogue search');return out.join('')}
  // The search line is one live region written only with a changed string, so a background poll
  // that finds the same counts does not re-announce them to a screen reader.
  function announce(text){if(!searchStatus)return;if(announcedText===text)return;announcedText=text;searchStatus.textContent=text}
  // Minimal and one-shot. A section already showing its heading and some content is left alone, and
  // reduced-motion users get an instant jump rather than a long smooth scroll.
  function revealNode(node){
    if(!node||typeof node.scrollIntoView!=='function')return;
    var viewport=window.innerHeight||(document.documentElement&&document.documentElement.clientHeight)||0;
    var rect=typeof node.getBoundingClientRect==='function'?node.getBoundingClientRect():null;
    if(rect&&rect.top>=0&&rect.top<=Math.max(0,viewport-120))return;
    var reduced=!!(window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches);
    try{node.scrollIntoView({behavior:reduced?'auto':'smooth',block:'start'})}catch(e){node.scrollIntoView(true)}
  }
  function focusCatalogSearch(){if(!search)return;revealNode(search.closest?(search.closest('.card')||search):search);try{search.focus()}catch(e){}}
  // Every dispatch gets one page-level generation. A section reports into it once, the counts are
  // rendered only when that whole generation has settled, and a response belonging to an earlier
  // generation is discarded rather than mixed into the line.
  // A keystroke supersedes whatever is in flight on the spot, not 250ms later when the debounce
  // fires: otherwise the old query's answers can land inside that window and replace the
  // acknowledgement with stale counts, render stale rows and scroll while the user is still typing.
  function invalidateAll(){tables.forEach(function(t){t.invalidate()});generationResults=null;revealGeneration=0}
  function beginQuery(options){
    var generation=++queryGeneration;
    activeGeneration=generation;generationResults=new Array(tables.length);generationSettled=0;
    revealGeneration=options&&options.reveal?generation:0;
    settledQuery=search?search.value.trim():'';
    var reset=!!(options&&options.reset);
    if(!tables.length){settleAll(generation);return Promise.resolve([])}
    return Promise.all(tables.map(function(t){return reset?t.reset(generation):t.load(generation)}));
  }
  function settleAll(generation){if(generation!==activeGeneration||!generationResults)return;var reveal=revealGeneration===generation;generationResults=null;revealGeneration=0;finishQuery(settledQuery,[],reveal)}
  function settleSection(generation,index,result){
    if(!generation||!generationResults||generationResults[index])return;
    generationResults[index]=result;
    if(++generationSettled<tables.length)return;
    var results=generationResults,reveal=revealGeneration===generation;
    generationResults=null;revealGeneration=0;
    finishQuery(settledQuery,results,reveal);
  }
  function finishQuery(query,results,reveal){
    if(!query){announce('');return}
    if(results.some(function(r){return !r.ok})){announce('Search results are temporarily unavailable.');return}
    var matched=results.filter(function(r){return r.total>0});
    if(!matched.length){announce('No entities match this search.');return}
    announce('Matches: '+results.map(function(r){return r.label+' '+r.total}).join(' \u00b7 '));
    if(!reveal)return;
    var target=null;matched.forEach(function(r){if(!target&&r.table==='suggested')target=r});
    revealNode((target||matched[0]).card);
  }
  function ownsMutation(claim){return mutationOwner===claim}
  function refreshMutationControls(){var owner=mutationOwner&&mutationOwner.name,busy=!!mutationOwner,s=lastStatus||{};syncButton.disabled=busy||!!s.sync_running;syncButton.textContent=owner==='scan'?'Scanning…':(s.sync_running?'Scanning…':'Scan dashboard now');resetButton.disabled=busy||!!s.sync_running;resetButton.textContent=owner==='reset'?'Resetting…':(s.sync_running?'Reset available after scan':'Reset learned data');autoStatic.disabled=autoRuntime.disabled=busy;issuesRescan.disabled=busy||!!s.sync_running;if(owner==='reset'){applyButton.disabled=true;applyButton.textContent='Reset in progress…'}else if(busy||s.sync_running){applyButton.disabled=true;applyButton.textContent=owner==='apply'?'Applying…':((owner==='scan'||s.sync_running)?'Scan in progress…':'Update in progress…')}else if(s.automatic_activation_blocked){applyButton.disabled=true;applyButton.textContent='Choose how to handle entity-discovery checks'}else if(!s.apply_required){applyButton.disabled=true;applyButton.textContent='Subscription up to date'}else if(s.activation_required&&!s.stream_change_required){applyButton.disabled=false;applyButton.textContent='Enable automatic updates'}else{applyButton.disabled=false;applyButton.textContent='Apply policy set ('+s.stream_entity_count+' → '+s.desired_count+')'}document.querySelectorAll('.entity-list select[data-id],.entity-list button[data-bulk],.entity-list button[data-all-candidates],.entity-issue-toggle').forEach(function(control){control.disabled=busy})}
  function claimMutation(name){if(mutationOwner)return null;var claim={name:name,id:++mutationSequence};mutationOwner=claim;refreshMutationControls();return claim}
  function releaseMutation(claim){if(!ownsMutation(claim))return;mutationOwner=null;if(lastStatus){autoStatic.checked=!!lastStatus.auto_static;autoRuntime.checked=!!lastStatus.auto_runtime}refreshMutationControls()}
  function mutationRequest(claim,url,options){if(!ownsMutation(claim))return Promise.reject(new Error('entity mutation ownership changed'));return fetch(url,options)}
  async function loadStatus(){
    try{var s=await fetch('/api/v1/dashboard/entities/sync',{cache:'no-store'}).then(function(r){return r.json()});
      suggestedCount=Number(s.suggested_count==null?s.candidate_count:s.suggested_count)||0;status.innerHTML='<b>'+esc(s.sync_running?'synchronizing':s.state)+'</b> · '+s.stream_entity_count+' entities actively subscribed'+(s.stream_mode==='unfiltered'?' (unfiltered)':'')+' / '+s.catalog_count+' catalogued · '+suggestedCount+' suggested'+(Number(s.blocking_issue_count)?' · <span class="hot">'+Number(s.blocking_issue_count)+' entity-discovery '+(Number(s.blocking_issue_count)===1?'check needs':'checks need')+' a choice</span>':'')+(Number(s.ignored_issue_count)?' · '+Number(s.ignored_issue_count)+' safety '+(Number(s.ignored_issue_count)===1?'check':'checks')+' ignored':'')+' · last scan '+age(s.last_sync_at)+(s.unresolved_count?' · <span class="muted" title="Static scanning could not map these dynamic templates or custom-card expressions to exact entity IDs. Concrete IDs later observed at runtime are persisted, then automatically subscribed or shown under Suggested according to the runtime promotion policy. This count describes source expressions and does not count down as IDs are observed.">'+s.unresolved_count+' dynamic expressions not statically resolvable</span>':'')+(s.error?' · <span class="hot">'+esc(s.error)+'</span>':'')+' · DB '+displayBytes(s.db_bytes);
      lastStatus=s;if(!mutationOwner){autoStatic.checked=!!s.auto_static;autoRuntime.checked=!!s.auto_runtime}refreshMutationControls();
      return s;
    }catch(e){status.textContent='Status unavailable';return null}
  }
  function controller(card,index){
    var sectionLabel=card.dataset.short||card.dataset.table||'',sectionTable=card.dataset.table||'';
    var body=card.querySelector('tbody'),msg=card.querySelector('.entity-msg'),prev=card.querySelector('.entity-prev'),next=card.querySelector('.entity-next');
    var selectPage=card.querySelector('.entity-select-page'),selectedMsg=card.querySelector('.entity-selected');
    var state={items:[],total:0,offset:0,sortKey:'entity_id',sortDir:1,request:0,selected:new Set()};
    function render(){
      body.innerHTML='';
      state.items.forEach(function(e){var tr=document.createElement('tr'),reason=e.reasons||((e.static?'dashboard ':'')+(e.runtime?'runtime':''))||(search.value.trim()?'catalogue search':'');
        tr.innerHTML='<td class="col-select"><input type="checkbox" class="entity-select" data-id="'+esc(e.entity_id)+'"'+(state.selected.has(e.entity_id)?' checked':'')+' aria-label="Select '+esc(e.entity_id)+'"></td><td class="col-entity"><code title="'+esc(e.entity_id)+'">'+esc(e.entity_id)+'</code></td><td class="col-access" title="1 minute / 1 hour / 1 day">'+ranked(e.access_1m,e.access_1m_rank,String)+' / '+ranked(e.access_1h,e.access_1h_rank,String)+' / '+ranked(e.access_1d,e.access_1d_rank,String)+'</td><td class="col-rate" title="1 minute / 1 hour / 1 day">'+ranked(e.rate_1m_bps,e.rate_1m_rank,rate)+' / '+ranked(e.rate_1h_bps,e.rate_1h_rank,rate)+' / '+ranked(e.rate_1d_bps,e.rate_1d_rank,rate)+'</td><td class="col-reason"><span class="reason-text">'+esc(reason)+'</span><span class="reason-icons">'+reasonIcons(e,reason)+'</span></td><td class="col-last">'+age(e.last_access)+'</td><td class="col-override"><select data-id="'+esc(e.entity_id)+'"><option value="auto">Auto</option><option value="pinned"'+(e.pinned?' selected':'')+'>Pinned</option><option value="forced_exclude"'+(e.excluded?' selected':'')+'>Excluded</option></select></td>';body.appendChild(tr)});
      card.querySelectorAll('th button').forEach(function(b){b.classList.toggle('sorted',b.dataset.sort===state.sortKey);b.dataset.dir=b.dataset.sort===state.sortKey?(state.sortDir>0?'asc':'desc'):''});
      var start=state.total?state.offset+1:0,end=Math.min(state.offset+state.items.length,state.total);msg.textContent='Showing '+start+'–'+end+' of '+state.total;
      prev.disabled=state.offset===0;next.disabled=state.offset+state.items.length>=state.total;
      selectPage.checked=state.items.length>0&&state.items.every(function(e){return state.selected.has(e.entity_id)});selectPage.indeterminate=!selectPage.checked&&state.items.some(function(e){return state.selected.has(e.entity_id)});selectedMsg.textContent=state.selected.size+' selected';refreshMutationControls();
    }
    function report(generation,ok){settleSection(generation,index,{ok:ok,total:ok?state.total:0,label:sectionLabel,table:sectionTable,card:card})}
    async function load(generation){var request=++state.request;try{var u='/api/v1/dashboard/entities?limit='+pageSize+'&offset='+state.offset+'&filter='+encodeURIComponent(card.dataset.filter)+'&q='+encodeURIComponent(search.value.trim())+'&sort='+encodeURIComponent(state.sortKey)+'&dir='+(state.sortDir>0?'asc':'desc');var d=await fetch(u,{cache:'no-store'}).then(function(r){return r.json()});if(request!==state.request)return;state.items=d.items||[];state.total=Number(d.total)||0;render();report(generation,true)}catch(e){if(request!==state.request)return;msg.textContent='Entity list unavailable';report(generation,false)}}
    body.addEventListener('change',async function(ev){var sel=ev.target;if(sel.matches('.entity-select')){if(sel.checked)state.selected.add(sel.dataset.id);else state.selected.delete(sel.dataset.id);render();return}if(!sel.matches('select[data-id]'))return;var claim=claimMutation('override');if(!claim){loadAll();return}try{var id=sel.getAttribute('data-id'),choice=sel.value,force=choice==='forced_exclude'&&confirm('Exclude '+id+' even if the dashboard depends on it?');if(choice==='forced_exclude'&&!force){await loadAll();return}var r=await mutationRequest(claim,'/api/v1/dashboard/entities/override',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({entity_id:id,override:choice,force:force})});if(!r.ok)alert(await r.text());await Promise.all([loadStatus(),loadAll()])}catch(e){alert(e&&e.message?e.message:'Entity override failed')}finally{releaseMutation(claim)}});
    selectPage.addEventListener('change',function(){state.items.forEach(function(e){if(selectPage.checked)state.selected.add(e.entity_id);else state.selected.delete(e.entity_id)});render()});
    async function bulk(choice,allCandidates){var ids=Array.from(state.selected);if(!allCandidates&&!ids.length){alert('Select at least one entity first.');return}var claim=claimMutation('bulk-override');if(!claim)return;try{var label=allCandidates?'all '+suggestedCount+' suggested entities':ids.length+' selected entities',force=false;if(choice==='forced_exclude'){force=confirm('Exclude '+label+'? This can break dashboard cards.');if(!force)return}else if(allCandidates&&!confirm('Pin '+label+'?'))return;var r=await mutationRequest(claim,'/api/v1/dashboard/entities/overrides',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({entity_ids:ids,all_candidates:!!allCandidates,override:choice,force:force})});if(!r.ok){alert(await r.text());return}state.selected.clear();await Promise.all([loadStatus(),loadAll()])}catch(e){alert(e&&e.message?e.message:'Bulk override failed')}finally{releaseMutation(claim)}}
    card.querySelectorAll('button[data-bulk]').forEach(function(b){b.addEventListener('click',function(){bulk(b.dataset.bulk,false)})});var all=card.querySelector('button[data-all-candidates]');if(all)all.addEventListener('click',function(){bulk('pinned',true)});
    card.querySelector('thead').addEventListener('click',function(ev){var b=ev.target.closest('button[data-sort]');if(!b)return;var key=b.dataset.sort;if(state.sortKey===key)state.sortDir=-state.sortDir;else{state.sortKey=key;state.sortDir=(key==='entity_id'||key==='reasons'||key==='override')?1:-1}state.offset=0;load(activeGeneration)});
    prev.addEventListener('click',function(){state.offset=Math.max(0,state.offset-pageSize);load(activeGeneration)});next.addEventListener('click',function(){state.offset+=pageSize;load(activeGeneration)});
    return {load:load,reset:function(generation){state.offset=0;return load(generation)},invalidate:function(){state.request++}};
  }
  var tables=Array.prototype.map.call(document.querySelectorAll('.entity-list'),controller);
  function loadAll(){return beginQuery({})}
  function resetAll(){return beginQuery({reset:true})}
  function searchAll(){return beginQuery({reset:true,reveal:true})}
  function issueSources(issue){var sources=Array.isArray(issue.source_locations)?issue.source_locations:(Array.isArray(issue.sources)?issue.sources:[]);if(!sources.length&&issue.source_location)sources=[issue.source_location];return sources}
  function loadIssues(){return fetch('/api/v1/dashboard/entities/issues',{cache:'no-store'}).then(function(r){if(!r.ok)throw new Error('issues unavailable');return r.json()}).then(function(d){
    var items=Array.isArray(d)?d:(d.items||[]),blocking=Number(d.blocking_issue_count)||items.filter(function(i){return !!i.blocking}).length,ignored=Number(d.ignored_issue_count)||items.filter(function(i){return !!i.ignored}).length,total=Number(d.dashboard_issue_count);if(!Number.isFinite(total)||total<items.length)total=items.length;
    issuesCard.classList.toggle('has-blocking',blocking>0);if(!total)issuesSummary.textContent='No entity-discovery compatibility notes detected by the last scan.';else if(blocking>0)issuesSummary.textContent=blocking+' '+(blocking===1?'check requires':'checks require')+' review before automatic updates can change the entity subscription'+(ignored?' · '+ignored+' previously allowed':'')+'. The dashboard and current subscription continue working.';else if(ignored>0)issuesSummary.textContent=ignored+' previously allowed '+(ignored===1?'check':'checks')+'. Entity discovery is active; no action is required.';else issuesSummary.textContent=total+' compatibility '+(total===1?'note':'notes')+'. Entity discovery is active; no action is required.';issuesList.innerHTML='';
    items.forEach(function(issue){var row=document.createElement('article'),disposition=issue.blocking?'blocking':(issue.ignored?'allowed':'advisory');row.className='entity-issue '+disposition;if(issue.fingerprint)row.dataset.fingerprint=String(issue.fingerprint);var limited=issue.type==='limited_support'||issue.type==='compatibility_gap'||issue.type==='runtime_coverage',severity=issue.ignored?'Allowed':(limited?'Limited coverage':(issue.blocking?'Automatic updates paused':'Compatibility note'));var view=issue.view_title||issue.view_path||issue.view||'Dashboard',card=issue.card_title||issue.card||'';var locations=issueSources(issue),rule=issue.rule_summary||issue.rule||'',candidate=issue.candidate_count==null?issue.potential_count:issue.candidate_count,limit=issue.limit==null?issue.safe_limit:issue.limit,reason=issue.reason||issue.message||'',recommendation=issue.recommendation||issue.suggested_edit||'';var allowable=!!(issue.fingerprint&&issue.ignorable!==false&&(issue.blocking||issue.ignored));row.innerHTML='<div class="entity-issue-head"><span class="entity-issue-severity">'+esc(severity)+'</span><b>'+esc(view)+(card?' · '+esc(card):'')+'</b></div>'+(locations.length?'<div class="entity-issue-locations"><span>Source</span> '+locations.map(function(v){return '<code>'+esc(v)+'</code>'}).join(' ')+'</div>':'')+(rule?'<div><span class="entity-issue-label">Rule</span> <code>'+esc(rule)+'</code></div>':'')+(!limited&&(candidate!=null||limit!=null)?'<div><span class="entity-issue-label">Candidate set</span> '+esc(candidate==null?'—':candidate)+' potential entities'+(limit==null?'':' · safe limit '+esc(limit))+'</div>':'')+(reason?'<p>'+esc(reason)+'</p>':'')+(recommendation?'<p class="entity-issue-recommendation"><b>Suggested edit:</b> '+esc(recommendation)+'</p>':'')+(allowable?'<div class="entity-issue-allow-note">Allowing this check never adds entities. It only lets automatic updates continue without them. To subscribe to one, find it with the catalogue search at the top of this page and set it to <b>Pinned</b>; a manual pin is kept until you remove it.</div><button type="button" class="pbtn entity-issue-search" data-search-focus="1">Search the entity catalogue</button>':'');if(allowable){var button=document.createElement('button');button.type='button';button.className='pbtn entity-issue-toggle';button.textContent=issue.ignored?'Re-enable safety check':'Ignore potential entities and continue';button.addEventListener('click',async function(){var claim=claimMutation('issue-ignore');if(!claim)return;try{if(!issue.ignored&&!confirm('Continue while omitting every potential entity from this configuration? This adds no entities to the subscription. Cards which depend on them may show unavailable until you pin those entities by hand, the dashboard is narrowed, or the safety check is re-enabled.'))return;var response=await mutationRequest(claim,'/api/v1/dashboard/entities/issues',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({fingerprint:String(issue.fingerprint),ignored:!issue.ignored})});if(!response.ok)alert(await response.text());await Promise.all([loadStatus(),loadIssues(),resetAll()])}catch(e){alert(e&&e.message?e.message:'Safety-check update failed')}finally{releaseMutation(claim)}});row.appendChild(button)}issuesList.appendChild(row)
    });
    var expressions=Array.isArray(d.dynamic_expressions)?d.dynamic_expressions:[];dynamicList.innerHTML='';dynamicSection.hidden=!expressions.length;expressions.forEach(function(expression){var row=document.createElement('article');row.className='entity-dynamic-expression';if(expression.fingerprint)row.dataset.fingerprint=String(expression.fingerprint);row.innerHTML='<div><span class="entity-issue-label">Source</span> <code>'+esc(expression.source_location||'Dashboard')+'</code></div><pre><code>'+esc(expression.literal||'')+'</code></pre>'+(expression.truncated?'<div class="muted">Expression shortened for diagnostics.</div>':'');dynamicList.appendChild(row)});
    refreshMutationControls();return d
  }).catch(function(){issuesSummary.textContent='Dashboard configuration issues are temporarily unavailable.';issuesCard.classList.remove('has-blocking')})}
  function action(message,kind){actionResult.textContent=message;actionResult.className='entity-action-result '+(kind||'muted')}
  function changed(a,b){return a.stream_mode!==b.stream_mode||a.stream_filter_hash!==b.stream_filter_hash}
  syncButton.addEventListener('click',async function(){var claim=claimMutation('scan');if(!claim)return;try{var before=await loadStatus();if(!before){action('Scan could not start because status is unavailable.','error');return}action('Scanning the HA catalogue and dashboard; the live subscription changes only if enabled policy finds a different set.','muted');var r=await mutationRequest(claim,'/api/v1/dashboard/entities/sync',{method:'POST'});if(!r.ok&&r.status!==409){action('Scan failed to start: '+await r.text(),'error');await loadStatus();return}var after=null;for(var i=0;i<120;i++){await new Promise(function(resolve){setTimeout(resolve,500)});after=await loadStatus();if(after&&!after.sync_running&&(after.last_sync_at!==before.last_sync_at||after.state==='degraded'))break}var outcome,outcomeKind;if(!after||after.sync_running){outcome='Scan is still running; status will continue updating above.';outcomeKind='error'}else if(after.state==='degraded'){outcome='Scan finished with an error: '+(after.error||'unknown error')+'. The previous live subscription was retained.';outcomeKind='error'}else if(after.automatic_activation_blocked){outcome='Scan complete: '+after.blocking_issue_count+' entity-discovery safety '+(Number(after.blocking_issue_count)===1?'check needs':'checks need')+' a choice. Home Assistant and the dashboard are still available; the previous '+after.stream_entity_count+'-entity live subscription was retained.';outcomeKind='error'}else{var stream=changed(before,after)?'live subscription changed from '+before.stream_entity_count+' to '+after.stream_entity_count:'live subscription unchanged at '+after.stream_entity_count;outcome='Scan complete: '+after.catalog_count+' catalogued, '+after.suggested_count+' suggested; '+stream+' entities.';outcomeKind='ok'}await Promise.all([loadStatus(),loadIssues(),resetAll()]);action(outcome,outcomeKind)}catch(e){action('Scan failed: '+(e&&e.message?e.message:'request unavailable'),'error')}finally{releaseMutation(claim)}});
  applyButton.addEventListener('click',async function(){var claim=claimMutation('apply');if(!claim)return;try{var s=await loadStatus();if(!s||!s.apply_required)return;var prompt=s.stream_change_required?'Replace the current '+s.stream_entity_count+'-entity stream with '+s.desired_count+' policy-selected entities? This adds '+s.pending_additions+', removes '+s.pending_removals+', and reloads the dashboard.':'Keep the current '+s.stream_entity_count+'-entity stream and enable future automatic policy updates?';if(!confirm(prompt))return;action('Applying the policy-selected subscription…','muted');var r=await mutationRequest(claim,'/api/v1/dashboard/entities/activate',{method:'POST',headers:{'Content-Type':'application/json'},body:'{"confirm":true}'});if(!r.ok){action('Apply failed: '+await r.text(),'error')}else{var d=await r.json();action(d.stream_changed?'Applied: '+d.previous_count+' → '+d.entity_count+' entities; added '+d.added_count+', removed '+d.removed_count+'. Dashboard reload requested.':'Automatic updates enabled; the '+d.entity_count+'-entity live stream did not need changing.','ok')}await Promise.all([loadStatus(),resetAll()])}catch(e){action('Apply failed: '+(e&&e.message?e.message:'request unavailable'),'error')}finally{releaseMutation(claim)}});
  resetButton.addEventListener('click',async function(){var claim=claimMutation('reset');if(!claim)return;var resultMessage='',resultKind='error';try{if(!confirm('Reset all learned entity evidence, manual pin/exclude overrides, and entity-discovery safety ignore decisions? This cannot be undone. The current live subscription will be kept while a replacement scan rebuilds the evidence.'))return;action('Resetting learned entity data while retaining the current live subscription…','muted');var r=await mutationRequest(claim,'/api/v1/dashboard/entities/reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirm:true,clear_filter:false})});if(!r.ok){resultMessage='Reset failed: '+await r.text()}else{var d=await r.json();resultMessage=d.sync_started?'Reset complete. Learned evidence, manual overrides, and safety ignore decisions were removed. A replacement scan is running; the current live subscription remains in place until that scan succeeds.':'Reset complete. Learned evidence, manual overrides, and safety ignore decisions were removed. The current live subscription was retained, but the replacement scan did not start; use Scan dashboard now to retry.';resultKind='ok';await Promise.all([loadStatus(),loadIssues(),resetAll()])}}catch(e){resultMessage='Reset failed: '+(e&&e.message?e.message:'request unavailable')}finally{await loadStatus();if(resultMessage)action(resultMessage,resultKind);releaseMutation(claim)}});
  async function savePolicy(){var claim=claimMutation('policy');if(!claim){await loadStatus();return}try{var r=await mutationRequest(claim,'/api/v1/dashboard/entities/policy',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({auto_static:autoStatic.checked,auto_runtime:autoRuntime.checked})});if(!r.ok)alert(await r.text());await Promise.all([loadStatus(),loadIssues(),resetAll()])}catch(e){alert(e&&e.message?e.message:'Policy update failed')}finally{releaseMutation(claim)}}
  autoStatic.addEventListener('change',savePolicy);autoRuntime.addEventListener('change',savePolicy);
  issuesList.addEventListener('click',function(ev){var t=ev&&ev.target;if(!t||typeof t.closest!=='function')return;if(!t.closest('.entity-issue-search'))return;focusCatalogSearch()});
  issuesRescan.addEventListener('click',function(){syncButton.click()});
  search.addEventListener('input',function(){clearTimeout(timer);invalidateAll();announce(search.value.trim()?'Searching\u2026':'');timer=setTimeout(searchAll,250)});
  loadStatus();loadIssues();loadAll();setInterval(function(){if(!document.hidden){loadStatus();loadIssues()}},5000);setInterval(function(){if(!document.hidden&&!document.querySelector('.entity-list select:focus'))loadAll()},10000);
})();
